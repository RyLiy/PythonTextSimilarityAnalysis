# PythonTextSimilarityAnalysis

Python backend using flask. Provides a similarity computation service between two strings of text.

pip install -r requirements.txt

Then cd to root of repo, and, "flask run"
